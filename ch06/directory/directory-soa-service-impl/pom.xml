<?xml version="1.0"?>
<project>
	<parent>
		<artifactId>directory-parent</artifactId>
		<groupId>com.manning.sdmia.ch06</groupId>
		<version>1.0.0</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<packaging>bundle</packaging>
	<artifactId>directory-soa-service-impl</artifactId>
	<name>Directory SOA Service Impl</name>
	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
			<resource>
				<directory>.</directory>
				<includes>
					<include>META-INF/**</include>
				</includes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<version>${felix.bundle.plugin.version}</version>
				<extensions>true</extensions>
				<configuration>
					<manifestLocation>META-INF</manifestLocation>
					<instructions>
						<Import-Package>
						org.springframework*;version=${spring.version},
						org.springframework.jdbc.datasource;version=${spring.version},
						org.springframework.aop;version=${spring.version},
						org.springframework.aop.framework;version=${spring.version},
						org.aopalliance.aop,
						com.manning.sdmia.directory*;version="${project.version}",
						javax.sql,
						*</Import-Package>
						<Export-Package></Export-Package>
						<Private-Package>com.manning.sdmia.directory.soa*</Private-Package>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<dependency>
			<groupId>com.manning.sdmia.ch06</groupId>
			<artifactId>directory-service</artifactId>
			<version>${project.version}</version>
		</dependency>	
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>org.springframework.jdbc</artifactId>
			<version>${spring.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>org.springframework.transaction</artifactId>
			<version>${spring.version}</version>
		</dependency>
	</dependencies>
</project>