<?xml version="1.0" encoding="UTF-8"?>
<project>
	<parent>
		<artifactId>directory-parent</artifactId>
		<groupId>com.manning.sdmia.ch06</groupId>
		<version>1.0.0</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<artifactId>directory-modular-dao-jdbc</artifactId>
	<packaging>bundle</packaging>
	<name>Directory modular DAO JDBC impl</name>
	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
			<resource>
				<directory>.</directory>
				<includes>
					<include>META-INF/**</include>
				</includes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<version>${felix.bundle.plugin.version}</version>
				<extensions>true</extensions>
				<configuration>
					<manifestLocation>META-INF</manifestLocation>
					<instructions>
						<Export-Package></Export-Package>
						<Import-Package>com.manning.sdmia*;version=${project.version},
						org.springframework*;version=${spring.version},
						javax.sql,
						*</Import-Package>
						<Private-Package>com.manning.sdmia.directory.modular.dao.jdbc</Private-Package>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<dependency>
			<groupId>com.manning.sdmia.ch06</groupId>
			<artifactId>directory-modular-dao</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>org.springframework.jdbc</artifactId>
			<version>${spring.version}</version>
		</dependency>
	</dependencies>

</project>